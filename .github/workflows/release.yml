# Disabled by default in this template repo.
#
# Publishes the package to npm via Changesets on every push to `main`.
# Will fail until you configure the required secrets and rename the package.
#
# To enable:
#   1. Set repo secrets (Settings → Secrets and variables → Actions):
#      - GH_ACCESS_TOKEN: GitHub PAT with `contents:write` & `pull-requests:write`
#      - NPM_TOKEN: npm publish token with access to your package scope
#   2. Rename `@repo/package` in `packages/package/package.json` to your real
#      package name, and update any `--filter @repo/package` references.
#   3. Uncomment every line below (remove the leading "# ").
#
# name: Release
#
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
#
# concurrency:
#   group: release-${{ github.ref }}
#   cancel-in-progress: false
#
# jobs:
#   release:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#       pull-requests: write
#       id-token: write
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v6
#         with:
#           fetch-depth: 0
#           token: ${{ secrets.GH_ACCESS_TOKEN }}
#
#       - name: Setup mise
#         uses: jdx/mise-action@v4
#
#       - name: Configure npm registry
#         uses: actions/setup-node@v6
#         with:
#           registry-url: 'https://registry.npmjs.org'
#
#       - name: Install pnpm
#         uses: pnpm/action-setup@v6
#
#       - name: Install Dependencies
#         run: pnpm install --frozen-lockfile
#
#       - name: Build Package
#         run: pnpm --filter @repo/package run build
#
#       - name: Create Release Pull Request or Publish to npm
#         id: changesets
#         uses: changesets/action@v1
#         with:
#           title: 'chore: version packages'
#           commit: 'chore: version packages'
#           version: pnpm changeset:version
#           publish: pnpm changeset:publish
#         env:
#           GITHUB_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
#           NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
#
#       - name: Create v-prefixed git tag
#         if: steps.changesets.outputs.published == 'true'
#         run: |
#           VERSION=$(node -p "require('./packages/package/package.json').version")
#           if git rev-parse "v${VERSION}" >/dev/null 2>&1; then
#             echo "Tag v${VERSION} already exists. Skipping."
#           else
#             git tag "v${VERSION}"
#             git push origin "v${VERSION}"
#           fi
